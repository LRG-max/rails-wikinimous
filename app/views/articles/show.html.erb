<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card border-0 shadow-sm rounded-3 p-4">
        <div class="card-body">

          <h2 class="display-5 fw-semibold text-secondary border-bottom pb-2 mb-4 text-center">
            📰 <%= @article.title %>
          </h2>

          <p class="text-dark fs-5"><%= @article.content %></p>

          <div class="d-flex justify-content-between mt-4">
            <%= link_to "🔙 Back", articles_path, class: "btn btn-outline-secondary rounded-pill px-4" %>

            <div>
              <%= link_to edit_article_path(@article), class: "btn btn-outline-secondary rounded-pill px-4 me-2" do %>
                <i class="fas fa-pencil-alt"></i> Edit
              <% end %>

              <%= link_to article_path(@article),
                data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                class: "btn btn-outline-danger btn-outline-soft-danger rounded-pill px-4" do %>
                <i class="fas fa-trash"></i> Delete
              <% end %>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>
