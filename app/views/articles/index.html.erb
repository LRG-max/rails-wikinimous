<div class="container mt-4">
  <div class="sticky-title">
    <h2 class="text-center display-5 fw-semibold text-secondary border-bottom pb-2">
      📖 Articles
    </h2>
  </div>

  <div class="articles-container mt-4">
    <div class="row">
      <% @articles.each do |article| %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-0 shadow-sm rounded-3 h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title">
                <%= link_to article.title, article_path(article), class: "text-decoration-none text-dark fw-bold" %>
              </h5>
              <p class="card-text text-muted">Publié le <%= article.created_at.strftime("%d %B %Y") %></p>

              <div class="mt-auto d-flex justify-content-between">
                <%= link_to edit_article_path(article), class: "btn btn-outline-secondary btn-sm rounded-pill px-3" do %>
                  <i class="fas fa-pencil-alt"></i>
                <% end %>

                <%= link_to article_path(article),
                  data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                  class: "btn btn-outline-danger btn-outline-soft-danger btn-sm rounded-pill px-3" do %>
                  <i class="fas fa-trash"></i>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<%= link_to "➕ New Article", new_article_path, class: "btn btn-outline-success btn-lg rounded-pill px-4 fw-semibold shadow-sm new-article-btn" %>
